# Ignore the default Rails log files.
log/*
tmp/*
.cache/

# Ignore the SQLite database file (if you're using SQLite).
db/*.sqlite3
db/*.sqlite3-journal

# Ignore the default Rails PID file.
tmp/pids/*

# Ignore the default Rails session store.
tmp/sessions/*

# Ignore by default all secret key files.
config/secrets.yml

# Ignore all backups of files.
*.bak
*.swp

# Ignore node_modules and Yarn lock file.
node_modules/
yarn-error.log

# Ignore .env files for local environment variables (if you're using dotenv).
.env

# Ignore the public/uploads directory if using CarrierWave or Paperclip for file uploads.
public/uploads

# Ignore the vendor directory (if you use bundler, vendored gems, etc.)
vendor/bundle

# Ignore coverage directories (if you use SimpleCov for test coverage).
coverage/

# Ignore any temporary files that can be generated by your text editor.
*.log
*.tmp
*.swp
